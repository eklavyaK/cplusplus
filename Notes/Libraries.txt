<iostream> - cin>>, cout<<, exit(0),

<algorithm> - for_each(), sort(), reverse(), *max_element(), *min_element(), count(), find(), erase(), binary_search(), upper_bound(), 
lower_bound(), __gcd()

<numeric> - accumulate(), iota(), gcd(), lcm(),

<cmath> - pow(), round(), max(), min(), sqrt(), cbrt(), abs(), ceil(), floor(), log10(), log(), cos(), sin(), tan().........so on

<string> - to_string(), stoi(), stol(), stoll(), stof(), stod(), stold(),

<utility> - pair<>, make_pair(), swap(), move(),

<cstdlib> - malloc(), free(), rand(), qsort(), NULL, bsearch(),

<iterator> - begin(), end(), advance(), next(), prev(), distance(),

<bitset> - bitset <no. of bits> (decimal val).to_string().

<climits> - CHAR_MIN, CHAR_MAX, INT_MIN, INT_MAX, LONG_MIN, LONG_MAX, LLONG_MIN, LLONG_MAX,

<cstdio> - printf, scanf,

<iomanip> - fixed, showpoint, setw(), setprecision()